# Tomcat
server:
  port: 9201

# Spring
spring:
  application:
    # 应用名称
    name: system
  profiles:
    # 环境配置
    active: dev
  cloud:
    nacos:
      discovery:
        # 服务注册地址
        server-addr: 139.180.192.129:8848
        #命名空间
        namespace: 4bdc41e2-506f-463a-bc75-6d5329fd83d1
      config:
        # 配置中心地址
        server-addr: 139.180.192.129:8848
        #命名空间
        namespace: 4bdc41e2-506f-463a-bc75-6d5329fd83d1
        # 配置文件格式
        file-extension: yml
        # 共享配置
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
